var React = require('react');

var TimerControls = React.createClass({

 render: function(){
   return (
     <div>
       <p> this is the TimerControls !</p>
     </div>
   );
 }

});

module.exportes = TimerControls;
